% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eventsurvey.R
\name{eventsurvey}
\alias{eventsurvey}
\title{Estimate effects of an event during survey fieldwork}
\usage{
eventsurvey(
  formula,
  data,
  event_time = 0,
  window = 6L,
  event_day = c("exclude", "include"),
  report_event_day = TRUE,
  alpha = 0.05,
  schedule = c("observed", "consecutive")
)
}
\arguments{
\item{formula}{A two-sided formula of the form \code{outcome ~ time}, using an
untransformed numeric outcome column and an untransformed time column.}

\item{data}{A data frame or tibble with one row per respondent.}

\item{event_time}{The event period, in the same scale and class as the time
variable. Defaults to \code{0}, which is appropriate when time is coded
relative to the event. Supply the event date or period explicitly when
using an absolute time scale.}

\item{window}{Integer length of both the final pre-event fitting window and
the post-event forecast horizon. Must be at least 2.}

\item{event_day}{Whether the event period is excluded so forecasting begins
one period later (\code{"exclude"}, the default) or included in the forecast
horizon (\code{"include"}). Exclusion is the safer default because exposure is
often ambiguous for respondents interviewed during the event period.}

\item{report_event_day}{If \code{FALSE}, an included event period remains in the
window-average estimate but is omitted from the printed day-specific
results and plots. This argument applies only when
\code{event_day = "include"}.}

\item{alpha}{Significance level for confidence intervals.}

\item{schedule}{How rolling fitting and forecasting windows are formed. The default,
\code{"observed"}, forms fitting and forecasting windows from ordered periods
containing responses and supports sparse survey schedules. \code{"consecutive"}
requires every period on the time scale to be represented. In either mode,
models use the actual time values rather than compressing gaps.}
}
\value{
An object of class \code{eventsurvey} containing the estimate, honest and
conventional intervals, day-specific results, reference forecast errors,
rolling-window diagnostics, automatic design and data checks, daily
summaries, and analysis settings.
}
\description{
\code{eventsurvey()} extrapolates the pre-event outcome trend into a short
post-event window. It uses rolling pre-event forecasts to bound possible
misspecification and constructs an honest confidence interval using the
union--intersection method.
}
\details{
The formula must contain untransformed column names for one numeric outcome
and one time variable, for example \code{support ~ interview_date}. Binary
outcomes coded 0/1, ordered responses coded with numeric values such as 1--5,
and continuous outcomes are supported when their period means are
substantively meaningful. Factor and character outcomes must first be
deliberately recoded as numeric. Time may be an integer-like numeric
variable, a \code{Date}, or a \code{POSIXt} value. The current implementation
deliberately uses a time-only linear model; transformations, interactions,
and covariates are not yet supported.

Every fitted object contains automatic diagnostics. A check receives
\code{"Review"} status when calendar periods are missing, any observed period has
fewer than 10 responses, a within-period variance must be pooled, fewer than
five rolling reference windows are available, or an observed-period window
spans more calendar periods than requested. These descriptive flags do not
alter estimates or invalidate an analysis.
}
\examples{
fit <- eventsurvey(y ~ day, sample_data)
fit
summary(fit)
}
\seealso{
\code{vignette("automatic-diagnostics", package = "eventsurvey")} for
examples of every automatic check and guidance on \code{Review} statuses.
}
