% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{eventsurvey_report}
\alias{eventsurvey_report}
\title{Create a complete event-survey report}
\usage{
eventsurvey_report(
  x,
  file = NULL,
  open = interactive(),
  overwrite = FALSE,
  title = "Event Survey Report"
)
}
\arguments{
\item{x}{An object of class \code{eventsurvey}, or a non-empty list of such
objects. Names identify analyses in a multi-analysis report; missing names
are replaced with \code{"Analysis 1"}, \code{"Analysis 2"}, and so on.}

\item{file}{Optional output path. When \code{NULL}, the report is written to a
temporary file. A \code{.html} extension is added when none is supplied.}

\item{open}{Whether to open the completed report automatically. The default,
\code{interactive()}, evaluates to \code{TRUE} when the function is called from an
interactive session such as the RStudio console, and normally evaluates
to \code{FALSE} in \code{Rscript}, tests, continuous integration, and other batch
runs. The HTML file is created either way.}

\item{overwrite}{Whether an existing output file may be replaced.}

\item{title}{Report title. Defaults to \code{"Event Survey Report"}.}
}
\value{
Invisibly, the normalized path to the generated HTML report.
}
\description{
\code{eventsurvey_report()} turns one fitted \code{\link[=eventsurvey]{eventsurvey()}} or
\code{\link[=eventsurvey_summary]{eventsurvey_summary()}} object, or a named list of fitted objects, into a
self-contained HTML report. Each analysis retains its own estimates,
diagnostics, settings, and figures.
}
\examples{
dat <- simulate_eventsurvey(seed = 10)

if (requireNamespace("rmarkdown", quietly = TRUE) &&
    rmarkdown::pandoc_available()) {
  report_file <- eventsurvey(y ~ day, dat) |>
    eventsurvey_report(open = FALSE)
}
}
