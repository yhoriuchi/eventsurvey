% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eventsurvey.R
\name{eventsurvey_summary}
\alias{eventsurvey_summary}
\title{Estimate event effects from period-level summaries}
\usage{
eventsurvey_summary(
  formula,
  data,
  n,
  variance,
  event_time,
  window = 6L,
  event_day = c("include", "exclude"),
  report_event_day = TRUE,
  alpha = 0.05,
  pre_periods = c("observed", "consecutive")
)
}
\arguments{
\item{formula}{A two-sided formula of the form \code{period_mean ~ time}.}

\item{data}{A data frame with one row per observed period.}

\item{n}{The unquoted name of the column containing respondent counts.}

\item{variance}{The unquoted name of the column containing the within-period
sample variance of the outcome. It may be \code{NA} when \code{n} is one; the
pooled within-period variance is then used for uncertainty calculations.}

\item{event_time}{The event period, in the same scale and class as the time
variable.}

\item{window}{Integer length of both the final pre-event fitting window and
the post-event forecast horizon. Must be at least 2.}

\item{event_day}{Whether the event period is included in the forecast
horizon (\code{"include"}) or skipped so forecasting begins one period later
(\code{"exclude"}).}

\item{report_event_day}{If \code{FALSE}, an included event period remains in the
window-average estimate but is omitted from the printed day-specific
results and plots. This is useful when exposure during the event period is
ambiguous.}

\item{alpha}{Significance level for confidence intervals.}

\item{pre_periods}{How rolling pre-event windows are formed. The default,
\code{"observed"}, forms fitting and forecasting windows from ordered periods
containing responses and supports sparse survey schedules. \code{"consecutive"}
requires every period on the time scale to be represented. In either mode,
models use the actual time values rather than compressing gaps.}
}
\value{
An object of class \code{eventsurvey}; see \code{\link[=eventsurvey]{eventsurvey()}}.
}
\description{
\code{eventsurvey_summary()} is the sufficient-statistics counterpart to
\code{\link[=eventsurvey]{eventsurvey()}}. It is useful when respondent-level observations cannot be
redistributed but each period's count, mean, and sample variance are
available. For a time-only linear model, it produces the same estimates as
the respondent-level function.
}
\examples{
dat <- simulate_eventsurvey(seed = 10)
daily <- aggregate(y ~ day, dat, function(z) {
  c(mean = mean(z), n = length(z), variance = var(z))
})
daily <- data.frame(
  day = daily$day, mean = daily$y[, "mean"],
  n = daily$y[, "n"], variance = daily$y[, "variance"]
)
fit <- eventsurvey_summary(mean ~ day, daily,
  n = n, variance = variance,
  event_time = 0, window = 6
)
}
